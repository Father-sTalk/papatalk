//enums
export enum ButtonType {
  default = "default",
  hover = "hover",
  disabled = "disabled",
}
export enum GenderType {
  male = "male",
  female = "female",
}
export enum ConfirmType {
  Agree = 1,
  Disagree = 0,
}
